import styled from "styled-components";

const LoginActivity = () => {
  return (
    <>
      <Wrapper>LoginActivity</Wrapper>
    </>
  );
};

const Wrapper = styled.div``;

export default LoginActivity;
